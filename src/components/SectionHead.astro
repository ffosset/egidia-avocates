---
import Eyebrow from './Eyebrow.astro';
interface Props { eyebrow?: string; title: string; lead?: string; align?: 'left' | 'center' }
const { eyebrow, title, lead, align = 'left' } = Astro.props;
---
<div class:list={['head', `a-${align}`]}>
  {eyebrow && <Eyebrow>{eyebrow}</Eyebrow>}
  <h2>{title}</h2>
  {lead && <p>{lead}</p>}
</div>

<style>
  .head { max-width: 780px; }
  .a-center { margin: 0 auto; text-align: center; }
  h2 {
    margin-top: var(--stack-tight);
    font-family: var(--font-display);
    font-size: var(--fs-h2);
    font-weight: var(--fw-bold);
    line-height: 1.2;
    color: var(--text-heading);
  }
  p {
    margin-top: var(--stack);
    font-size: var(--fs-lead);
    line-height: 1.5;
    color: var(--text-muted);
    max-width: 640px;
  }
</style>
