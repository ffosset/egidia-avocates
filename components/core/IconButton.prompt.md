Icon-only control for closing, menus and toolbars — always 48px or larger.

```jsx
<IconButton icon="menu" label="Ouvrir le menu" />
<IconButton icon="x" label="Fermer" variant="outline" />
```

Notes: never use for a primary action a visitor must find; `label` is mandatory.
