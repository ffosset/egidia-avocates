Switches between sibling views inside one page (e.g. steps of a procedure).

```jsx
<Tabs items={[{id:'ofpra',label:'OFPRA'},{id:'cnda',label:'CNDA'}]} value={tab} onChange={setTab} />
```

Notes: max 4 tabs. Active tab is a 3px brun rule, not a filled pill.
