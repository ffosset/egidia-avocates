Mount once, at the top of the page: draws the two fixed margin rules that frame every Egidia screen.

```jsx
<PageRules />
```

Notes: the rules are the brand's most visible structural device — they must cross every section, light or dark, without interruption. Never hide them behind a full-bleed block; never inset content past them.
